import { FilterRowDirective } from './filter-row.directive';

describe('FilterRowDirective', () => {
  it('should create an instance', () => {
    const directive = new FilterRowDirective();
    expect(directive).toBeTruthy();
  });
});
