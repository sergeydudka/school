import { FilterRowFieldDirective } from './filter-row-field.directive';

describe('FilterRowFieldDirective', () => {
  it('should create an instance', () => {
    const directive = new FilterRowFieldDirective();
    expect(directive).toBeTruthy();
  });
});
